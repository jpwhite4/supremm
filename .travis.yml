language: python
python:
  - "2.6"
  - "2.7"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libpcp3-dev
install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script: pytest
